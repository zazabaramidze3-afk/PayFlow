# 🚀 Discount Toggle System - Implementation & Roadmap

ეს დოკუმენტი აღწერს "მოლარეებისთვის ფასდაკლების უფლების მართვის" სისტემის არქიტექტურას, რეალიზებულ ფაილებს და პროექტის სამომავლო განვითარების ეტაპებს.

---

## 🛠️ 1. რა არის დანერგილი (Current Status)

სისტემა წარმატებით არის ინტეგრირებული Full-stack არქიტექტურაში (PostgreSQL -> Node.js/Express -> React).

### 🗄️ მონაცემთა ბაზა (Database)
* **მიგრაცია:** `migration_add_discount.sql`
* **ცვლილება:** `public.users` ცხრილში დაემატა ახალი სვეტი `can_use_discount BOOLEAN NOT NULL DEFAULT true`.

### 🧠 ბექენდი (Backend)
* **ავტორიზაცია (`backend/src/routes/auth.ts`):** 
  * `GET /api/me` ენდპოინტი მომხმარებლის სესიის ჩატვირთვისას აბრუნებს `can_use_discount` სტატუსს.
  * `GET /api/users` (მომხმარებელთა სია) აბრუნებს ახალ ველს ადმინისტრაციული პანელისთვის.
* **უფლებების მართვა (`backend/src/routes/auth.ts`):**
  * დაინერგა `PUT /users/:id/discount-access` ენდპოინტი.
  * დაემატა როლების ვალიდაცია: მხოლოდ `admin`-ს ან `manager`-ს შეუძლია უფლებების შეცვლა (სხვა შემთხვევაში ბრუნდება `403 Forbidden`).
* **უსაფრთხოების ფილტრი (`backend/src/routes/sales.ts`):**
  * `POST /payments` ენდპოინტში ჩააშენდა Server-side შემოწმება. თუ მოლარე (`cashier`) ცდილობს ტრანზაქციაში ფასდაკლების გატარებას მაშინ, როცა მისი უფლება გამორთულია, სერვერი ბლოკავს მოთხოვნას (`403 Forbidden`).

### 💻 ფრონტენდი (Frontend)
* **როუტინგი და მენიუ (`frontend/src/App.tsx`):**
  * მენეჯერის (`MANAGER`) როლს გაეხსნა წვდომა `Users Control` გვერდზე.
* **მართვის პანელი (`frontend/src/pages/UsersManagement.tsx`):**
  * **მენეჯერის ფილტრი:** თუ სისტემაში მენეჯერია შესული, ის ცხრილში ხედავს **მხოლოდ `cashier` როლის მქონე მომხმარებლებს**. ადმინები და სხვა მენეჯერები მისთვის დამალულია.
  * დაემატა ინტერაქტიული ჩექბოქსი "ფასდაკლების უფლება", რომელიც ავტომატურად ინახავს სტატუსს ბაზაში და არ იშლება რეფრეშისას.
* **გაყიდვების ეკრანი (`frontend/src/pages/Sales.tsx`):**
  * ფასდაკლების `select` მენიუ და თანხის `input` ველი ავტომატურად ხდება `disabled`.
  * დაინერგა `useEffect` დაცვა (ავტომატური Reset საწყის 0%-ზე უფლების ჩამორთმევისთანავე) და `handleCheckout` ვალიდაცია `toast` შეტყობინებით.

### 🎯 აუდიტის ლოგები (Audit Logging System) ✅
* **ბაზა (`backend/src/index.ts` → `initDB()`):** დაემატა ცხრილი `audit_logs (id, actor_id, target_id, action, new_value, created_at)`. ცხრილი ავტომატურად იქმნება ყოველ სერვერის სტარტზე, დანარჩენი ცხრილების ანალოგიურად — ცალკე ხელით მიგრაცია არ საჭიროებია.
* **ბექენდი (`backend/src/routes/auth.ts`):**
  * `PUT /users/:id/discount-access` ყოველ წარმატებულ გამოძახებაზე წერს ჩანაწერს `audit_logs`-ში (`actor_id` = ცვლილების ავტორი, `target_id` = ვისაც შეეცვალა უფლება, `new_value` = ახალი მნიშვნელობა). ლოგირების შეცდომა არ აჩერებს/არ არღვევს რეალურ განახლებას.
  * დაინერგა `GET /api/audit-logs` — აბრუნებს ბოლო 50 ჩანაწერს (actor/target სახელებთან და target-ის როლთან ერთად join-ით), წვდომა მხოლოდ `admin`/`manager`-ისთვის.
* **ფრონტენდი (`frontend/src/pages/UsersManagement.tsx`):**
  * დაემატა "🕘 History" ღილაკი, რომელიც შლის/ხსნის ისტორიის პანელს და თან ჩატვირთავს `GET /audit-logs`-ს.
  * მენეჯერისთვის ისტორიის სია იმავე წესით არის გაფილტრული, რაც მომხმარებელთა ცხრილი — მხოლოდ `cashier` როლთან დაკავშირებული ჩანაწერები ჩანს.
  * ისტორიის სია ჩასმულია `max-height: 300px` + `overflow-y: auto` კონტეინერში — ბევრი ლოგი აღარ აჩოჩებს გვერდს დაბლა, ბოლო ჩანაწერი ყოველთვის ზემოთაა.
* **ლოგირების გაფართოება:** `writeAuditLog` საერთო ჰელფერი ახლა ორივე toggle-ენდპოინტს ემსახურება — `PUT /users/:id/history-access`-იც წერს ჩანაწერს (`action: 'history-access'`), არა მხოლოდ `discount-access`. ისტორიის პანელი `action`-ის მიხედვით არჩევს სწორ ქართულ ტექსტს ("ფასდაკლების უფლება" vs "ისტორიის ნახვის უფლება").

---

## 🔮 2. სამომავლო განვითარების გეგმა (Next Steps Roadmap)

ეტაპი 1 დასრულებულია. დარჩენილია შემდეგი 2 ეტაპი:

### 🔐 ეტაპი 2: მენეჯერის დროებითი ავტორიზაცია (Manager Override Pin)
* **მიზანი:** თუ მოლარეს უფლება გამორთული აქვს, მაგრამ კლიენტი ითხოვს ფასდაკლებას, მენეჯერს შეეძლოს იქვე, გაყიდვების ეკრანზევე დაადასტუროს ეს ქმედება.
* **ფრონტენდი:** ბლოკირებულ ფასდაკლების ღილაკზე დაჭერისას ამოხტეს მოდალური ფანჯარა: *"შეიყვანეთ მენეჯერის PIN კოდი"*.
* **ბექენდი:** შეიქმნას სწრაფი ვალიდაციის ენდპოინტი მენეჯერის პინის შესამოწმებლად, რომელიც კონკრეტულ ტრანზაქციაზე ერთჯერადად დაუშვებს ფასდაკლებას.

### 📊 ეტაპი 3: ანალიტიკა და ლიმიტები (Discount Analytics & Thresholds)
* **მიზანი:** ფინანსური ზარალის თავიდან არიდება.
* **ფუნქციონალი:** უფლების სრულად ჩაკეტვის ნაცვლად, მენეჯერს შეეძლოს დააწესოს მაქსიმალური ლიმიტი (მაგ. *"ამ მოლარეს შეუძლია მაქსიმუმ 10%-იანი ფასდაკლების მიცემა"*).
* **ბექენდი:** `can_use_discount` ბულიან ველის ნაცვლად დაინერგოს `max_discount_percentage INT DEFAULT 10`.

---
_დოკუმენტი განახლდა ბოლო სტაბილური ბილდის მიხედვით: 2026 წლის 7 აგვისტო (ეტაპი 1 დასრულებულია)._

#პრომტ მუშაობის გასაგრძელებლად:
#პროექტის root დირექტორიაში არსებული DISCOUNT_TOGGLE_ROADMAP.md ფაილი აღწერს სტატუსს და სამომავლო გეგმას,
#ამ ფიჩერზე მუშაობა გრძელდება — შემდეგი ეტაპია #2 (Manager Override Pin) ან #3 (Discount Limits).
