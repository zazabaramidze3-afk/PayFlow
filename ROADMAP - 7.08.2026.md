# 🗺️ PayFlow Project - Discount & Rights Management Roadmap

## 📅 მიმდინარე სტატუსი: ეტაპი 1 დასრულებულია (ტესტირებული)

---

## ✅ რა გაკეთდა (Completed Features)

### 1. ბეკენდის აუდიტორული ლოგირება (Audit Logging)
* ბაზაში წარმატებით შეიქმნა `audit_logs` სტრუქტურა მოქმედებების ჩასაწერად.
* შეიქმნა დამხმარე ფუნქცია `writeAuditLog` ცვლილებების დასაფიქსირებლად.
* ინტეგრირდა `PUT` ენდფოინთებთან უფლებების ცვლილებისას.

### 2. ფრონტენდის ისტორიის პანელი (UI Components)
* შეიქმნა ფუნქციონალური პანელი სათაურით **„უფლებების ცვლილების ისტორია“**.
* **ვიზუალური ოპტიმიზაცია:** დაემატა ფიქსირებული სიმაღლე (`max-h-60` / `300px`) და ვერტიკალური სქროლი (`overflow-y-auto`), რათა ბევრმა ლოგმა გვერდი არ გადატვირთოს.
* ლოგების სორტირება ხდება ქრონოლოგიურად — უახლესი ჩანაწერები ყოველთვის ზემოთაა.

### 3. ახალი უფლებების მხარდაჭერა & მონაცემების სინქრონიზაცია
* დაემატა **„ისტორიის ნახვის უფლების“** ჩართვა/გამორთვის ლოგირება (`history-access`).
* გასწორდა მონაცემების რეალურ დროში განახლების ბაგი (`Re-fetching`) — ჩეკბოქსზე დაჭერისთანავე ფრონტენდი მომენტალურად ტვირთავს ახალ ლოგს სწორი ტექსტური აღწერით.

### 🧱 ეტაპი 1.5: არქიტექტურული გამართვა და უსაფრთხოება
- [ ] **ბაზის ინიციალიზაციის ოპტიმიზაცია (Production-Ready Migration):**
  - წაიშალოს ცხრილების ავტომატური შექმნის ლოგიკა კოდიდან (`initDB()`-დან `CREATE TABLE IF NOT EXISTS`), რათა პროდაქშენზე ავიცილოთ თავიდან მონაცემების გადაწერის/ბლოკირების რისკები.
  - შეიქმნას ოფიციალური მიგრაციის ფაილი (`.sql` ფაილი) PostgreSQL-ისთვის ბაზის სტრუქტურის ხელით გასამართად (PgAdmin-ში გასაშვებად ან CI/CD სკრიპტისთვის).

- [ ] **ლოგების სრული გასუფთავების ღილაკი (მხოლოდ Admin-ისთვის):**
  - **Backend:** შეიქმნას დაცული `DELETE /api/audit-logs` ენდფოინთი, რომელიც მხოლოდ `ADMIN` როლის მომხმარებელს მისცემს ბაზიდან ლოგების წაშლის უფლებას.
  - **Frontend:** ისტორიის პანელის ზედა კუთხეში დაემატოს წითელი ღილაკი „ისტორიის გასუფთავება“. ღილაკი გამოჩნდეს მხოლოდ იმ შემთხვევაში, თუ სისტემაში ავტორიზებული მომხმარებელი არის `ADMIN`. დაჭერისას ჩაირთოს დამატებითი დასტურის ფანჯარა (`Confirm`).

### 🔑 ეტაპი 2: მენეჯერის პინ-კოდით ავტორიზაცია (Manager PIN Override)
- [ ] **მონაცემთა ბაზის მოდიფიკაცია:** მომხმარებლების ცხრილში დაემატოს ახალი სვეტი `manager_pin` (4 ან 6 ციფრიანი კოდებისთვის).
- [ ] **API ენდფოინთი:** შეიქმნას ბეკენდზე პინ-კოდის ვალიდაციის სერვისი, რომელიც გადაამოწმებს შეყვანილი PIN-ის სისწორესა და მენეჯერის უფლებამოსილებას.
- [ ] **ფრონტენდის ინტერფეისი:** გაყიდვების/გადახდის ეკრანზე, თუ მოლარეს (Cashier) არ აქვს ფასდაკლების უფლება, სისტემამ ავტომატურად ამოაგდოს ფანჯარა (Modal Popup) ტექსტით: *"საჭიროა მენეჯერის ავტორიზაცია"*. მენეჯერის მიერ სწორი პინის შეყვანის შემდეგ, ფასდაკლება ერთჯერადად დაუშვას ამ კონკრეტულ ტრანზაქციაზე.
