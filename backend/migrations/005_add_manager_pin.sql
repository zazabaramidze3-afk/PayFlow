-- ==========================================
-- Migration 005: Manager PIN Override
-- ==========================================
-- ემატება public.users.manager_pin — VARCHAR სვეტი, სადაც ინახება
-- 4-ციფრიანი მენეჯერის PIN-კოდის bcrypt ჰეში (არასდროს — plain text).
-- bcrypt ჰეშის სიგრძე ფიქსირებულია 60 სიმბოლოზე, ამიტომ VARCHAR(60)
-- საკმარისია, მაგრამ მომავალი bcrypt ვერსიების/ალგორითმის ცვლილების
-- მოსაქნელად ვტოვებთ მცირე მარაგს — VARCHAR(100).
--
-- სვეტი NULL-ადი და DEFAULT NULL-ია: PIN არ ენიჭება ავტომატურად
-- არცერთ მომხმარებელს — მხოლოდ ADMIN-ს შეუძლია დაუყენოს კონკრეტულ
-- მენეჯერს PUT /api/users/:id/pin ენდპოინტიდან (Users Control პანელი).
-- NULL == "ამ მომხმარებლისთვის PIN ჯერ არ არის დაყენებული".
--
-- IF NOT EXISTS-ის წყალობით უსაფრთხოა გაშვება მაშინაც, თუ ეს
-- სვეტი უკვე დამატებულია.
-- ==========================================

BEGIN;

ALTER TABLE public.users
  ADD COLUMN IF NOT EXISTS manager_pin VARCHAR(100) DEFAULT NULL;

COMMIT;
